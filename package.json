{
  "name": "capstone",
  "version": "1.0.0",
  "scripts": {
    "build": "parcel build src/index.html --public-url /your-repo-name",
    "lint": "eslint .",
    "test": "jest",
    "deploy": "npx run build && gh-pages -d dist",
    "ci": "npx run lint && npx run test && npx run deploy"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@jest/globals": "^29.5.0",
    "@parcel/transformer-sass": "^2.8.3",
    "eslint": "^8.41.0",
    "jest": "^29.5.0",
    "lodash": "^4.17.21",
    "parcel": "^2.8.3"
  },
  "dependencies": {
    "eslint-config-standard": "^17.0.0",
    "jsdom": "^22.0.0"
  },
  "description": ""
}
